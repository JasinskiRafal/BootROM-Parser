[package]
name = "bootrom-parser"
version = "0.1.0"
edition = "2024"
description = "Simple BootROM traces parser"

[dependencies]
clap = { version = "4.5.57", features = ["derive"] }
strum_macros = "0.27.2"
